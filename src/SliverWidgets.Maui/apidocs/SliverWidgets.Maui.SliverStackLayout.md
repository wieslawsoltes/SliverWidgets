---
uid: SliverWidgets.Maui.SliverStackLayout
---

# Summary
MAUI `Layout` that arranges children as a fixed-extent sliver stack.

# Remarks
This layout is backed by an `ILayoutManager`. It is not a general item recycling surface; use `SliverCollectionView` for large item counts.
