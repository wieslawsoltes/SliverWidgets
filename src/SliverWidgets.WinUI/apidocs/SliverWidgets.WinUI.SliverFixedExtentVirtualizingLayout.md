---
uid: SliverWidgets.WinUI.SliverFixedExtentVirtualizingLayout
---

# Summary
WinUI `VirtualizingLayout` for fixed-extent `ItemsRepeater` rows.

# Remarks
The layout uses `VisibleRect` and `RealizationRect` to build sliver constraints and arrange native `ItemsRepeater` elements.
