---
uid: SliverWidgets.Core.SliverAdvancedPersistentHeaderLayout
---

# Summary
Adds floating and snap behavior to the persistent header layout model.

# Remarks
The layout uses `SliverPersistentHeaderState` and an `ISliverHeaderSnapAnimationService` so snapping behavior can be tested without a framework animation clock.
