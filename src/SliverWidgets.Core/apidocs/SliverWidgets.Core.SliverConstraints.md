---
uid: SliverWidgets.Core.SliverConstraints
---

# Summary
Describes the current viewport, scroll, paint, cache, and axis state for one sliver layout pass.

# Remarks
Constraints are local to the sliver currently being laid out. `ScrollOffset` is relative to that sliver, while `PrecedingScrollExtent` records how much content came before it in the composed viewport.
